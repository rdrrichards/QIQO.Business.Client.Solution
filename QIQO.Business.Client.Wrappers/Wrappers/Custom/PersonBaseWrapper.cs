using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;

namespace QIQO.Business.Client.Wrappers
{
    public partial class PersonBaseWrapper
    {
        //public override IEnumerable<ValidationResult> Validate(ValidationContext validationContext)
        //{
        //    if (string.IsNullOrWhiteSpace(PersonCode))
        //    {
        //        yield return new ValidationResult("A person must have a code to identify them",
        //          new[] { nameof(PersonCode) });
        //    }

        //    if (string.IsNullOrWhiteSpace(PersonFirstName))
        //    {
        //        yield return new ValidationResult("A person must have a first name",
        //          new[] { nameof(PersonFirstName) });
        //    }

        //    if (string.IsNullOrWhiteSpace(PersonLastName))
        //    {
        //        yield return new ValidationResult("A person must have a last name",
        //          new[] { nameof(PersonLastName) });
        //    }
        //}
    }
}
